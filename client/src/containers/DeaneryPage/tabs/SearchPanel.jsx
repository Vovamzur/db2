import React from 'react';

// key, name, subject, position, 
const academics = []

// key, name, class,
const students = []

const SearchPanel = () => {
  return (
    <div>
      SearchPanel
    </div>
  )
}

export default SearchPanel;
